{
  "name": "dn404",
  "scripts": {
    "compile": "npx hardhat compile",
    "local_node": "npx hardhat node",
    "deploy_local": "npx hardhat run --network localhost script/deploy.js",
    "deploy_sepolia": "npx hardhat run --network sepolia script/deploy.js",
    "deploy_base": "npx hardhat run --network base script/deploy.js"
  },
  "license": "MIT",
  "version": "0.0.19",
  "description": "Implementation of a co-joined ERC20 and ERC721 pair.",
  "files": [
    "src/**/*.sol"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vectorized/dn404.git"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-foundry": "^1.1.1",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.7",
    "dotenv": "^16.4.5"
  }
}
